Badge — tiny all-caps accent pill for the product mode label in a panel header.

```jsx
<Badge>BUILD-UP</Badge>
```

Always blue accent on a soft accent fill. Keep the text short and uppercase. Use non-breaking hyphens in two-word labels (BUILD‑UP, WIND‑DOWN).
